# Legacy workflow kept for manual runs only.
# Use ci.yml for the primary CI pipeline.

name: Python application (legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated; use ci.yml instead."
