# 🪶 MyCoder Ultra-Lightweight Dockerfile
# Minimální spotřeba zdrojů pro slabší hardware
FROM python:3.11-slim

# Install jen nejnutnější system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Install Ollama (lightweight)
RUN curl -fsSL https://ollama.ai/install.sh | sh

# Create app directory
WORKDIR /app

# Install jen základní Python packages (žádné dev tools)
RUN pip install --no-cache-dir \
    aiohttp \
    asyncio

# Copy jen nutné soubory
COPY src/ ./src/
COPY docker-entrypoint.lightweight.sh ./
RUN chmod +x docker-entrypoint.lightweight.sh

# Lightweight user setup
RUN useradd -m -u 1000 mycoder && \
    chown -R mycoder:mycoder /app && \
    mkdir -p /app/workspace /app/logs && \
    chown -R mycoder:mycoder /app/workspace /app/logs

# Expose ports
EXPOSE 8000 11434

# Lightweight health check
HEALTHCHECK --interval=60s --timeout=10s --start-period=90s --retries=2 \
    CMD curl -f http://localhost:11434/api/tags || exit 1

USER mycoder

# Environment pro úsporu zdrojů
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
ENV OLLAMA_MAX_LOADED_MODELS=1
ENV OLLAMA_NUM_PARALLEL=1
ENV OLLAMA_MAX_VRAM=0

CMD ["./docker-entrypoint.lightweight.sh"]

# 📊 OPTIMALIZACE PRO SLABŠÍ HW:
#
# ✅ Velikost image: ~2GB (místo 5GB+)
# ✅ RAM footprint: ~1.5GB (místo 4GB+)
# ✅ CPU usage: Minimal (single-threaded)
# ✅ No build tools: Rychlejší build
# ✅ No dev dependencies: Méně overhead
# ✅ TinyLlama only: Nejmenší AI model
# ✅ CPU-only inference: Žádná GPU potřeba
# ✅ Single model loading: Úspora paměti
#
# ⚠️  Trade-offs:
# - Jednodušší AI responses
# - Pomalejší generování
# - Základní funkcionalita jen
# 
# 🎯 Ideální pro:
# - Hardware s 4-8GB RAM
# - 2-4 CPU cores
# - Testování a učení
# - Lehký development